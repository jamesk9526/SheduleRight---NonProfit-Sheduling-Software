{
  "name": "@packages/workflows",
  "version": "0.0.1",
  "description": "Twilio flows, reminders, and workflow orchestration",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest"
  },
  "dependencies": {
    "@packages/schema": "workspace:*",
    "twilio": "^4.10.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
