# ScheduleRight Server Configuration
# Copy this file to .env and update with your values

# ======================
# Server Configuration
# ======================
NODE_ENV=development
SERVER_PORT=3001
SERVER_URL=http://localhost:3001
API_VERSION=v1

# ======================
# Database Provider
# ======================
# Options: couchdb | mysql
DB_PROVIDER=couchdb

# ======================
# Database (CouchDB)
# ======================
# CouchDB connection URL
COUCHDB_URL=http://localhost:5984

# CouchDB admin credentials
# WARNING: Use strong passwords in production!
COUCHDB_USER=admin
COUCHDB_PASSWORD=changeme

# ======================
# Database (MySQL)
# ======================
# Only used when DB_PROVIDER=mysql
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=scheduleright
MYSQL_USER=app_user
MYSQL_PASSWORD=app_password

# ======================
# Redis (Session/Cache)
# ======================
REDIS_URL=redis://localhost:6379

# ======================
# Authentication & Security
# ======================
# JWT secret for signing tokens
# IMPORTANT: Generate a secure random string (min 32 characters)
# Example: openssl rand -base64 32
JWT_SECRET=your-secret-key-change-in-production-min-32-chars

# JWT token expiration (in seconds)
# 900 = 15 minutes
JWT_EXPIRY=900

# Refresh token expiration (in seconds)
# 604800 = 7 days
REFRESH_TOKEN_EXPIRY=604800

# ======================
# CORS Configuration
# ======================
# Comma-separated list of allowed origins
CORS_ORIGIN=http://localhost:3000,http://localhost:3001

# ======================
# Twilio (SMS/Voice) - Optional
# ======================
# Required for SMS notifications and phone verification
# Sign up at: https://www.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=
TWILIO_VERIFY_SID=

# ======================
# Email (SMTP) - Optional
# ======================
# Required for email notifications
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# ======================
# Monitoring & Logging
# ======================
# Log level: debug, info, warn, error
LOG_LEVEL=info

# OpenTelemetry (observability)
OTEL_ENABLED=false

# Sentry error tracking (optional)
# Sign up at: https://sentry.io
SENTRY_DSN=

# ======================
# Production Notes
# ======================
# 1. Never commit .env files to version control
# 2. Use strong, unique passwords for all services
# 3. Generate JWT_SECRET with: openssl rand -base64 32
# 4. Set NODE_ENV=production in production
# 5. Use HTTPS URLs in production (https://)
# 6. Restrict CORS_ORIGIN to your production domain(s)
# 7. Enable monitoring tools in production (Sentry, OTEL)
# 8. Regularly rotate secrets and credentials
# 9. Use environment-specific .env files (.env.production, .env.staging)
# 10. Store production secrets in a secure vault (AWS Secrets Manager, HashiCorp Vault, etc.)
